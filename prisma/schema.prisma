generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Agent {
  id     String @id @default(cuid())
  name   String
  author String
  role   String?

  @@index([author])
}
